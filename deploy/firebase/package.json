{
  "name": "ymk-autobuy-firebase-hosting",
  "private": true,
  "scripts": {
    "build": "node ./build.mjs",
    "deploy": "npm run build && firebase deploy --only hosting",
    "serve": "npm run build && firebase serve --only hosting"
  },
  "devDependencies": {
    "firebase-tools": "^14.0.0"
  }
}
